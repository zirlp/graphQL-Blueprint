const CountryList = () => {
  //   useEffect(() => {
  //     setCountryList(
  //       data?.countries.filter((country) => country.continent.name === continent)
  //     );
  //   }, [continent]);
  //   return (
  //     <div>
  //       {countryList?.map((country) => (
  //         <div>
  //           <h4>{`${country.name}, ${country.emoji}`}</h4>
  //           {/* <p>{country.capital}</p> */}
  //         </div>
  //       ))}
  //     </div>
  //   );
};

export default CountryList;
